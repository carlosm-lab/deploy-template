# =============================================================================
# Dependencies - Flask Deploy Template (Production)
# =============================================================================
# Run: pip install -r requirements.txt
# Audit: pip-audit
# Last updated: 2026-01-20
# =============================================================================

# Core Framework
Flask==3.1.2
Werkzeug==3.1.5
Jinja2==3.1.6

# Security & Forms
flask-wtf==1.2.2

# Rate Limiting (con soporte Redis para producción)
flask-limiter==3.8.0
redis==5.0.1

# Configuration
python-dotenv==1.2.1

# =============================================================================
# Notas de Seguridad:
# - Redis es requerido para rate limiting en producción (Vercel serverless)
# - Configura REDIS_URL o UPSTASH_REDIS_REST_URL en variables de entorno
# =============================================================================
